package model.enum

//The purpose of ElectronicsType is to provide options for selecting the category for the electronics product, and it does this by using enum(no instantiation needed).
//Mirrors FoodType.kt
enum class ElectronicsType {
    HEADPHONES,
    EARBUDS,
    POWER_BANK,
    USB_CABLE,
    CHARGER,
    MOBILE_ACCESSORY
}
