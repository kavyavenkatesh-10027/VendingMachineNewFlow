package model.enum

//The purpose of FoodType is to provide options for selecting the category for the food product, and it does this by using enum(no instantiation needed).
enum class FoodType {
    CHIP,
    WATER_BOTTLE,
    COLD_DRINK,
    ENERGY_DRINK,
    MILK_SHAKE,
    CHOCOLATE_BAR,
    CHEWING_GUM,
    JELLY
}