package model.enum

//The purpose of ProductCategory is to let the Admin flow / ProductFactory dispatch on "which kind of product" and hence call the respective service
enum class ProductCategory {
    FOOD,
    ELECTRONIC
}
